import models.Library;

/**
 * This is the main class that starts the Library Management System.
 *
 * @author John
 */
public class Main {
    /**
     * The main method is the entry point of the application.
     * It calls the start method of the Library class to start the Library Management System.
     *
     * @param args command-line arguments passed to the program. Not used in this application.
     */
    public static void main(String[] args) {
        Library.start();
    }
}
